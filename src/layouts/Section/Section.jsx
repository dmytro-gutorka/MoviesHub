import './Section.scss'
import classNames from 'classnames'


const Section = (props) => {
    const {
        className,
        title,
        titleId,
        description,
        actions,
        children,
    } = props

    return (

    )
}

export default Section